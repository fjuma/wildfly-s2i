build:
  uri: https://github.com/wildfly/wildfly-s2i
  contextDir: examples/elytron-oidc-client
deploy:
  env:
    # once RH SSO is configured, specify the WildFly Realm URL here
    #- name: SERVER_ARGS
    #  value: -Dorg.wildfly.s2i.example.oidc.provider-url=https://<host of the SSO provider>/auths/realms/WildFly
    